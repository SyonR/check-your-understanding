Takes the same repository model + synthesized curriculum.

Generates one final quiz.

The important instruction should be something like:

Test whether the learner understands relationships and can navigate/change the system, not whether they memorized names.

That gives you:

ordering
scenario
where-would-you-change-this
dependency reasoning
flow completion
multiple choice

instead of trivia.